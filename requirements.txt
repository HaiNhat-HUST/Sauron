# --- Core / connectors ---
pydantic>=2.6          # config / data models
pydantic-settings>=2.2 # env-driven settings per connector
httpx>=0.27            # async HTTP client used by every connector
feedparser>=6.0        # RSS / Atom parsing (security blogs)
python-dotenv>=1.0     # load .env in local dev

# --- Web API (FastAPI SPA backend) ---
fastapi>=0.110         # async JSON API
uvicorn[standard]>=0.29  # ASGI server

# --- Storage (PostgreSQL + ChromaDB) ---
SQLAlchemy[asyncio]>=2.0  # async ORM
psycopg[binary]>=3.1      # PostgreSQL driver (psycopg3, sync + async)
chromadb>=0.5             # vector DB for article-content embeddings (contextual search)

